// const R = require("ramda");
// const sinon = require("sinon");

describe('Something', () => {
  describe('not exists', () => {
    it('should pass as if nothing has happened', () => {
      expect(0).to.equal(0);
    });
  });

  describe('with a fake server', () => {
    it('should act as the real one', () => {});
  });
});
